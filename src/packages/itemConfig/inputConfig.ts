import type { IConfigItem } from "src/types/formTypes";

const inputConfig: IConfigItem[] = [
  {
    label: "默认值",
    field: "defaultValue",
    widgets: "BaseInput"
  }
];

export default inputConfig;
